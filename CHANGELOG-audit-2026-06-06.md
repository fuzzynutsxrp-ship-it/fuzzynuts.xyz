# CHANGELOG — Repo Audit & Cleanup (2026-06-06)

All changes below are **local commits on `main`**. Nothing was pushed to any remote.
**Safety anchors:** pre-reorg checkpoint `9adab3cb47838fe8869500e9333c2201e77ed068` · full tarball `~/project-backups/project-audit-backup-2026-06-06-1612.tar.gz` (sha256 `433cc7eb…5aa0`).
**Net:** 232 files changed, 72 insertions(+), 104,641 deletions(−) — of which 207 deletions are the de-tracked (not deleted-from-disk) `public/games` build output.

## Commits (oldest → newest)

| SHA | Message |
|-----|---------|
| `9adab3c` | pre-reorg-checkpoint (empty marker) |
| `b0dc1e6` | chore(repo): de-track generated public/games build output (~30MB) |
| `174103d` | chore(repo): remove orphaned Dockerfiles, stray npm lockfile, dead gh-pages workflow, empty stub workspaces |
| `462a842` | docs(env): align .env.example templates with actual env usage |
| `95c0fca` | docs: relocate loose incident/deploy notes into how-to/ and runbooks/; de-dup archive |
| `716930f` | docs: fix internal links to relocated guides |
| `146600a` | chore(turbo): drop dangling test:e2e task with no implementing workspace |
| `85b27b0` | chore(turbo): prune stale env vars from build cache config |

## Detail by batch

### Batch 1 — de-track generated game output (`b0dc1e6`)
- `git rm -r --cached apps/web-arcade/public/games` (207 files removed from git index; **files remain on disk**, regenerated by `pnpm build:games`).
- Re-tracked the two `.gitignore` exceptions: `public/games/.gitkeep`, `public/games/rsc/index.html`.
- Effect: future commits stop carrying ~30 MB of regenerated binaries (incl. `golf.wasm` 18 MB). *History still contains them — a history rewrite was out of scope.*

### Batch 2 — orphan purge (`174103d`)
Deleted (tracked): `Dockerfile.api`, `apps/api/Dockerfile` (both byte-identical, unused — Railway builds via nixpacks), `apps/web-arcade/package-lock.json` (npm leftover in a pnpm-only repo), `.github/workflows/deploy-gh-pages.yml` (dead/broken — `npm ci` blocked by `only-allow pnpm`).
Removed (empty stub dirs, untracked): `packages/eslint-config`, `packages/shared-ui`, `packages/testing`, `tools/codemod`.

### Batch 3 — `.env.example` rewrite (`462a842`)
- Root `.env.example`: `MONGO_URL`→`MONGODB_URI`; dropped unused `XUMM_API_KEY`; replaced the real-looking (leaked) `GAME_SESSION_SECRET` value with a generate-me placeholder; documented previously-missing server vars (`OPENAI_API_KEY`, `DISCORD_WEBHOOK_URL`, `RSC_PASSWORD_SECRET`, `VPS_ACCOUNT_SECRET`/`URL`, `ADMIN_WALLET_ADDRESS`, `SCORE_HMAC_SECRET`, `SERVER_SECRET`, `ALLOW_SINGLE_SIG_PAYOUT`, pricing/emission vars).
- `apps/web-arcade/.env.example`: added `NEXT_PUBLIC_API_BASE`, `NEXT_PUBLIC_PROJECT_ID`, `NEXT_PUBLIC_SSE`, `NEXT_PUBLIC_ADMIN_WALLET`, `NEXT_PUBLIC_CHAT_API`, `NEXT_PUBLIC_SECURE_SITE_ORIGIN`.

### Batch 4 — docs reorg + link fixes (`95c0fca`, `716930f`)
- `git mv` (renames, zero content change): 5 files → `docs/runbooks/` (fix-autologin, fix-failed-fetch, fix-railway-crash, railway-deploy-debug, live-smoke-test); 7 files → `docs/how-to/` (deploy-account-server, deploy-autologin, deploy-autologin-fix, secure-auth-deploy, vps-setup, railway-deploy-checklist, vercel-deploy-checklist).
- Removed duplicate `docs/_archive/original-archive/fuzzynuts_handoff.md` (stale "Kaetram" copy; kept the renamed `legacy-web-arcade` version).
- Fixed 5 internal links to the relocated guides in `README.md`, `.github/COMMIT_GUIDE.md` (×2), `docs/runbooks/live-smoke-test.md`, `.hermes-recovery.md`. Verified zero broken links remain (excl. frozen `_archive/`).
- `docs/` root now holds only the status-generator files (`README.md`, `STATUS.md`, `STATUS.md.tmpl`, `_status-state.yml`).

### Batch 5 — turbo `test:e2e` removal (`146600a`)
- Removed the `test:e2e` task from `turbo.json` and the `test:e2e` script from root `package.json` (no workspace implemented it).
- Deleted empty `tests/e2e-cross-app/` (only `.gitkeep`).

### Turbo tidy (`85b27b0`)
- Pruned stale `MONGO_URL` and `XUMM_API_KEY` from `turbo.json` `build.env`.

### Batch 6 — stale local branches (no commit; ref changes)
Deleted 9 merged local branches (recoverable via recorded SHAs and `remotes/origin/*`):
`headers-crisp-fix` `eacbf0b` · `headers-crisp-v2` `878d747` · `headers-crisp-v3` `b71fa36` · `kill-glassmorphism` `91ac72e` · `degen-overhaul` `e870454` · `hero-bg-degen-crush` `f5479d0` · `fuzzybear-mobile-degen` `f346cde` · `ui-token-fixes` `97d84b8` · `migration/monorepo` `50a6ef5`. Remaining local branch: `main`.

## Verification performed
- `pnpm install` — clean (13 workspaces resolve).
- `npx turbo run build --dry` — pipeline resolves across all 12 packages.
- `npx turbo run test:e2e --dry` — correctly errors "Could not find task" (confirms removal).
- Link-integrity grep — zero broken links in active codebase.

## Known pre-existing issues (NOT introduced by this audit; left untouched)
- `@fuzzynuts/mobile-capacitor` typecheck fails (`TS6059` — `capacitor.config.ts` outside declared `rootDir`).
- `@fuzzynuts/api` build fails (`TS` strict-null in `apps/api/src/routes/rsc.ts` `jwtVerify`).
- `@fuzzynuts/desktop-tauri` build needs a Rust/cargo toolchain (absent in audit sandbox).
- Sandbox runs Node 18.19.1; repo wants Node ≥20 (engine warnings only).

## Not done (out of scope / requires push or dashboards)
- Git **history** rewrite to purge large blobs (separate, riskier op).
- Remote branch deletions, Dependabot PRs, VPS teardown, secret rotation — covered in `EXTERNAL_CLEANUP_ACTION_PLAN.md` (Phase 5).
