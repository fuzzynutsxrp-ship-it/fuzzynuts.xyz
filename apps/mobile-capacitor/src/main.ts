/**
 * Mobile bootstrap — placeholder for future deep-link wiring
 * (e.g., handling the Xumm `xumm://` return-URL after a SignIn).
 *
 * The web app is loaded directly via webDir; this file only
 * runs if we add a custom WebView entrypoint later.
 */

export {};
