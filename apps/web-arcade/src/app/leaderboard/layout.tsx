/**
 * Leaderboard route layout — minimal wrapper.
 * TopNav + Sidebar are rendered inside the client component.
 */

export default function LeaderboardLayout({ children }: { children: React.ReactNode }) {
  return children;
}
