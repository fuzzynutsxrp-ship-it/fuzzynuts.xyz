{
  "name": "@fuzzynuts/games-build",
  "version": "0.1.0",
  "private": true,
  "description": "Single Vite-driven bundler for every iframe game. Replaces the historical fuzzynuts-games-dev / public/games dual-tree (which had drifted). Emits content-hashed assets into apps/web-arcade/public/games/<slug>/.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.mjs --watch",
    "build": "node scripts/build.mjs",
    "build:one": "node scripts/build.mjs --game",
    "clean": "rm -rf ../web-arcade/public/games/* && find . -name 'dist' -type d -prune -exec rm -rf {} +",
    "typecheck": "tsc --noEmit",
    "lint": "echo '(games-build uses repo-root eslint)'",
    "test": "echo '(no tests yet — game-specific tests live alongside each game)'"
  },
  "dependencies": {
    "@fuzzynuts/arcade-core": "workspace:*",
    "@fuzzynuts/shared-anticheat": "workspace:*"
  },
  "devDependencies": {
    "@fuzzynuts/tsconfig": "workspace:*",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
